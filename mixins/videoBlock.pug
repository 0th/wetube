
mixin videoBlock(video={})
   .videoBlock
      a(href=routes.videoDetails(video.id))
        video.vidoeBlock__thumbnail(src=video.videoFIle, controls= true)
        h4.vidoeBlock__title= video.title
        h6.videoBlcok__views= video.views
        
